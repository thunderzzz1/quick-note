# QuickNote v0.1 手工验收清单

运行方式：项目根目录执行 `npm run tauri dev`（首次编译约 3~5 分钟）。

以下 10 项全部通过才算 v0.1 完成。每项勾选 `[x]` 并记录实际结果。

## 1. 首启数据目录向导

- [ ] 首次启动出现"选择数据目录"页面
- [ ] 选择 D 盘（或其他非系统盘）目录后，该目录生成 `config.json` 和 `quicknote.db`
- [ ] 再次启动不再出现向导

实际结果：

## 2. 全局快捷键呼出与自动保存

- [ ] 按 `Alt+Shift+N` 呼出悬浮捕获窗
- [ ] 输入中文 Markdown（标题/列表/加粗），失焦（点击其他窗口）后自动保存
- [ ] `notes/YYYY/MM/` 下出现对应的 `.md` 文件，文件名形如 `20260808-153012-ab12.md`
- [ ] 再次呼出窗口继续输入并失焦，**不产生重复笔记**（同一草稿只对应一个文件）

实际结果：

## 3. 图片粘贴

- [ ] 截图后 Ctrl+V 粘贴进捕获窗
- [ ] 图片出现在 `attachments/YYYY/MM/<note_id>/img_001.png`
- [ ] Markdown 中出现 `![图片](attachments/...)` 引用
- [ ] 主界面打开该笔记，图片可正常渲染

实际结果：

## 4. Esc 隐藏与托盘

- [ ] 按 Esc 保存并隐藏捕获窗
- [ ] 系统托盘出现 QuickNote 图标
- [ ] 托盘菜单"打开 QuickNote"显示主窗口
- [ ] 托盘菜单"退出"正常退出

实际结果：

## 5. 主界面今日速记

- [ ] 主窗口"今日速记"列出当天全部记录（含未整理）
- [ ] 点击记录，右侧显示 Markdown 全文
- [ ] 搜索框可按标题过滤

实际结果：

## 6. AI 整理（手动触发）

- [ ] 设置页填入 DeepSeek API Key（base URL 与模型保持默认），保存
- [ ] 主界面"整理建议"页显示当天待整理记录
- [ ] 手动触发"立即整理"（设置页或整理页入口）后，建议按分类分组展示
- [ ] 每条建议显示原文、摘要、关键词

实际结果：

## 7. 确认落地

- [ ] 接受一条"待办"建议后，该记录出现在侧边栏"待办"分类
- [ ] 原始 `.md` 文件内容未改变
- [ ] "跳过"的记录不再出现在待确认列表
- [ ] "全部接受"一次处理当天全部建议

实际结果：

## 8. AI 提议新分类

- [ ] 记录内容明显不属于现有分类（例如"想买 Cherry 键盘，求推荐"）时，AI 提议新分类"购物清单"
- [ ] 接受后，侧边栏出现"购物清单"分类，且后续整理会优先复用
- [ ] 有效分类达到 10 个后，新内容归入"其他"

实际结果：

## 9. 更改数据目录与迁移

- [ ] 设置页点击"更改…"选择新目录
- [ ] 旧目录的配置、数据库、笔记、图片复制到新目录
- [ ] 重启应用后从新目录读取，数据完整

实际结果：

## 10. 重建索引

- [ ] 删除 `quicknote.db` 后启动（或在设置中触发"重建索引"）
- [ ] 记录从 `.md` 文件恢复，正文可读，图片引用有效

实际结果：
